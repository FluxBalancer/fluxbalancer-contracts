syntax = "proto3";

package metrics;

message Ack {
    bool ok = 1;
}

message NodeMetrics {
    string node_id = 1;
    int32 port = 2;
    double cpu_util = 3;
    double mem_util = 4;
    double net_in_bytes = 5;
    double net_out_bytes = 6;
}

service MetricsService {
    rpc PushMetrics (NodeMetrics) returns (Ack);
}